spring:
  application:
    name: auth-service
  server:
    port: 8081

jwt:
  secret: "centralized-secret-CHANGE-ME"
  expiration: 172800000 # 2 days

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true
